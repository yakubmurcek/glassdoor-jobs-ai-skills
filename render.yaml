services:
  - type: web
    name: ai-skills-backend
    env: python
    plan: free
    buildCommand: pip install .
    startCommand: uvicorn ai_skills.server:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PORT
        value: 8000
