# Core OpenAI credentials (required for OpenAI, optional for Ollama)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# Model Provider: "openai" (default) or "ollama"
MODEL_PROVIDER=openai

# OpenAI settings (used when MODEL_PROVIDER=openai)
OPENAI_MODEL=gpt-5-mini
OPENAI_TEMPERATURE=0.1

# Ollama settings (used when MODEL_PROVIDER=ollama)
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_MODEL=gemma3:12b

# Rate limiting and batching
RATE_LIMIT_DELAY=0.1
OPENAI_BATCH_SIZE=20
OPENAI_MAX_PARALLEL_REQUESTS=3

# Optional input/output overrides (defaults live under data/)
DATA_DIR=/absolute/path/to/data
INPUTS_DIR=/absolute/path/to/inputs
OUTPUTS_DIR=/absolute/path/to/outputs
INPUT_CSV=/absolute/path/to/input.csv
OUTPUT_CSV=/absolute/path/to/output.csv

# Optional processing limits
MAX_JOB_DESC_LENGTH=8000
